require 'spec_helper'

describe UploadController do
	it "accepts a file" do
		
	end

	it "submits a form properly" do
	end

	it "renders errors to user" do
	end

	it "properly calls the script to upload data (see product import tests)" do
	end
end