# Circle CI config
machine:
  environment:
    CAPYBARA_WAIT_TIME: 30
dependencies:
  pre:
    - "sudo apt-get update; sudo apt-get install libjpeg62; sudo apt-get install ghostscript; sudo apt-get install phantomjs"
checkout:
  post:
    - cp config/application.yml{.example,}
