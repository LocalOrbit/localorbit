<div class="tab-header stickable">
  <div class="l-constraint">
    <%= link_to "< Return to Discounts List", admin_discounts_path, class: "redundant-nav" %>
    <div class="l-page-header">
      <% if @discount.persisted? %>
        <h1>Update <%= @discount.name %></h1>
        <%= link_to raw('&#57418;'), [:admin, @discount], class: "delete font-icon",
          data: {method: :delete, confirm: "Are you sure you want to permanently delete this promotion?"}, title: "Delete" %>
      <% else %>
        <h1>Add Discount</h1>
      <% end %>
    </div>
  </div>
</div>
