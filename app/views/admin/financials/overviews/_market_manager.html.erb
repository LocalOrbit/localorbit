<div class="overview-intro">
  <p>This is a list of all money currently owed to your organization and that you owe to other organizations.</p>
</div>
<div class="manager-filters pull-left">
  <div class="pull-left">
    <%= search_form_for @q, url: admin_financials_overview_path, html: { class: "invoice_search clear-after" } do |f| %>
    <% if current_user.multi_market_membership? %>
      <span class="preserve-alignment">
        <%= f.label :market_id_in, "Market" %><br>
        <%= f.collection_select :market_id_in, @search_presenter.selling_markets, :id, :name, {include_blank: nil}, {multiple: true, class: [append_sticky_class(:market_id_in, ""),'multiselect'] } %>
      </span>
      <%= link_to "Reset", admin_financials_overview_path(clear: ''), class: 'btn reset' if can_reset?(@query_params) %>
  <% end %>
  <% end %>

  </div>
</div>
<%= render "money_in" %>
<%= render "money_out" %>
