<h1>Add Market Address</h1>

<%= render 'shared/errors', { resource: @address } %>

<%= form_for [:admin, @address] do |f| %>
  <%= render 'form', {f: f} %>
  <div>
    <%= f.submit "Add Address" %>
  </div>
<% end %>
