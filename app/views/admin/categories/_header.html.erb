<div class="tab-header stickable">
  <div class="l-constraint">
    <%= link_to "< Return to Product Taxonomy", admin_categories_path, class: "redundant-nav" %>
    <div class="l-page-header">
      <h1><i class="font-icon" data-icon="&#xe023;">&nbsp;</i>
        <% if @category.persisted? %>
          <%= @category.self_and_ancestors.pluck(:name).join(" / ") %>
        <% else %>
          Add a new category
        <% end %>
      </h1>
    </div>
  </div>
</div>