csv << ["Name", "Subdomain", "Contact Name", "Contact Phone"]

@markets.each do |market|
  csv << [
    market.name,
    market.ascii_subdomain,
    market.contact_name,
    market.contact_phone
  ]
end
