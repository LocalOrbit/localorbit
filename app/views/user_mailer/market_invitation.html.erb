<h1>Join <%= @market.name %> today!</h1>

<p>
  Hello! You have been added as a Market Manager for <%= @market.name %>.
</p>

<div class="lo_call_to_action">
  <a href="<%= root_url(host: (@market ? @market.domain : "app.#{Figaro.env.domain}")) %>" class="lo_button lo_button_large">View <%= @market.name %></a>
  <p>
    If clicking the button doesn't work, right click it and copy the link.<br>
    After you've copied it, paste it into a new browser window.
  </p>
</div>
