<%# 
Local variables:
  @subscriber: the subscribed market in question
  @event_params: the event.data.object for the event
%>
<p>Good day,</p>

<p>Stripe has reported a <span style="color:green">successful invoice payment</span> for '<%= @subscriber.name %>' (Stripe id: '<%= @event_params[:customer] %>')</p>

<p>Details:</p>
<ul>
  <li>Invoice id: <%= @event_params.id %></li>
  <li>Amount: <%= ::Financials::MoneyHelpers.cents_to_amount(@event_params.total) %></li>
  <% if @event_params.try(:subscription).present? %>
    <li>Subscription id: <%= @event_params.subscription %></li>
  <% end %>
</ul>
