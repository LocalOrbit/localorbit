<%# Message from publisher to subscriber - in other words: recipient is Subscriber, sender is Publisher %>
<p>Hello <%= @recipient.contact_name %>,

<h2>You have a new Cross Selling List waiting for you!</h2>

<p><%= @sender.name %> has shared the cross selling list '<%= @list_in_question.name %>' with you.</p>

<p>To subscribe to this list and set any or all of the products live in your catalog,</p>

<div class="lo_call_to_action">
  <%= link_to "Review List", admin_market_cross_selling_list_url(@recipient, @list_in_question, host: (@recipient ? @recipient.domain : "app.#{Figaro.env.domain}")), class: "lo_button lo_button_large" %>
  <p>
    If clicking the button doesn't work, right click it and copy the link.<br>
    After you've copied it, paste it into a new browser window.
  </p>
</div>
