<%# gets the market for the email layout. Isn't ideal, but works well. %>
<% if @market = @resource.default_market
     market_name = @market.name
   else
     market_name = "Local Orbit"
   end %>

<p>Hello <%= @resource.email %>!</p>

<p>Someone has requested a link to change your password. You can do this through the link below.</p>

<p><%= link_to 'Change my password', edit_password_url(@resource, :reset_password_token => @token, host: (@market ? @market.domain : "app.#{ENV.fetch('DOMAIN')}")) %></p>

<p>If you didn't request this, please ignore this email.</p>
<p>Your password won't change until you access the link above and create a new one.</p>
