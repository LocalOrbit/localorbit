<h1>Join <%= @org_or_market_name %> today!</h1>

<p>
  Hello! You have been invited to join <%= @org_or_market_name %>.
</p>

<div class="lo_call_to_action">
  <%= link_to "Join #{@org_or_market_name}", accept_invitation_url(@resource, :invitation_token => @token, host: (@market ? @market.domain : "app.#{Figaro.env.domain}")), class: 'lo_button lo_button_large'  %>
  <p>
    If clicking the button doesn't work, right click it and copy the link.<br>
    After you've copied it, paste it into a new browser window.
  </p>
</div>